name: Python CSV Reader Demo
build:
  commands:
    - pip install -r requirements.txt
    - pip install Flask==2.0.2 Werkzeug==2.0.0 # Use the same versions from requirements.txt
run:
  web: gunicorn -b 0.0.0.0:${PORT:-5000} app:app
